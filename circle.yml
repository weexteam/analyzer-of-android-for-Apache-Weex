machine:
  java:
    version: 'oraclejdk8'
test:
  override:
   - cd weex_analyzer && ./run-circle-ci.sh
  post:
    - cp -r weex_analyzer/build/reports $CIRCLE_ARTIFACTS/reports
    - cp -r weex_analyzer/build/outputs $CIRCLE_ARTIFACTS/outputs
dependencies:
  cache_directories:
    - "weex_analyzer/.gradle"
    - ~/.m2
